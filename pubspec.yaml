name: aether_app
description: "Aether - Cyber Security Censorship Circumvention Android Client"
publish_to: "none"
version: 1.2.0+3

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  http: ^1.2.1
  path_provider: ^2.1.3
  flutter_svg: ^2.0.10+1
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo/
    - assets/logo/aether_logo.png
    - assets/logo/aether_logo.svg
    - assets/bin/
